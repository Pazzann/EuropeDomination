using Godot;

namespace EuropeDominationDemo.Scripts.Scenarios.Goods.Weapon;

public class Armor : Good
{
    public Armor(int id, string name, Vector3 color): base(id, name, color)
    {
        
    }
}