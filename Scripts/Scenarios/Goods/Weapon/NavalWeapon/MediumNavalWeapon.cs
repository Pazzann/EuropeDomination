using Godot;

namespace EuropeDominationDemo.Scripts.Scenarios.Goods.Weapon.NavalWeapon;

public class MediumNavalWeapon : NavalWeapon
{
    public MediumNavalWeapon(int id, string name, Vector3 color) : base(id, name, color)
    {
        
    }   
}