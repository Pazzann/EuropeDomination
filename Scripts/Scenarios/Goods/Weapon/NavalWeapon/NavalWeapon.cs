using Godot;

namespace EuropeDominationDemo.Scripts.Scenarios.Goods.Weapon.NavalWeapon;

public class NavalWeapon : Weapon
{
    public NavalWeapon(int id, string name, Vector3 color) : base(id, name, color)
    {
        
    }   
}