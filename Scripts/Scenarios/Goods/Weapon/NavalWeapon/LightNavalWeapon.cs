using Godot;

namespace EuropeDominationDemo.Scripts.Scenarios.Goods.Weapon.NavalWeapon;

public class LightNavalWeapon : NavalWeapon
{
    public LightNavalWeapon(int id, string name, Vector3 color) : base(id, name, color)
    {
        
    }   
}