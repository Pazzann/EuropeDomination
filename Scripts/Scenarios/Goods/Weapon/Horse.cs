using Godot;

namespace EuropeDominationDemo.Scripts.Scenarios.Goods.Weapon;

public class Horse : Good
{
    public Horse(int id, string name, Vector3 color): base(id, name, color)
    {
        
    }
}